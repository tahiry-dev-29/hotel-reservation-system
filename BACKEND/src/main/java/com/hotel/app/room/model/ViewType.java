package com.hotel.app.room.model;

/**
 * Represents the type of view available from the room.
 */
public enum ViewType { // Added ViewType enum
    GARDEN,
    SEA,
    CITY,
    MOUNTAIN,
    NONE
}